apiVersion: v1
kind: Service
metadata:
  name: {{ include "vibecast.fullname" . }}
  labels:
    {{- include "vibecast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vibecast.selectorLabels" . | nindent 4 }}